version: "3.9"

services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./uploads:/app/uploads
      - ./quizify.db:/app/quizify.db
    environment:
      - PYTHONUNBUFFERED=1